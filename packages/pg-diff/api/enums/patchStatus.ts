import { PatchStatus } from '../types';

export const patchStatus = {
	TO_APPLY: PatchStatus.TO_APPLY,
	IN_PROGRESS: PatchStatus.IN_PROGRESS,
	DONE: PatchStatus.DONE,
	ERROR: PatchStatus.ERROR,
};

export default patchStatus;
